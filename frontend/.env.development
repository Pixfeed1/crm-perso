# Configuration pour le mode développement local
# SQL.js (SQLite dans le navigateur) est utilisé

# Ne PAS définir REACT_APP_API_URL pour utiliser SQL.js en local
# REACT_APP_API_URL=

# En mode développement, le frontend utilise SQL.js + IndexedDB
# Toutes les données sont stockées localement dans le navigateur
